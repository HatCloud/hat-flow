# hatflow-dispatching-parallel-agents

并行代理调度。当面对 2+ 个无共享状态、无顺序依赖的独立任务时，使用此 skill 同时派发多个 agent 并行工作。

## 触发词

- "并行执行"
- "同时处理"
- "hatflow-dispatching-parallel-agents"

## 适用场景

- 多个独立的代码搜索/分析任务
- 多个不相关文件的同时编辑
- 并行运行多个验证/检查流程
