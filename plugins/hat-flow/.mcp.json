{
  "mcpServers": {
    "linear": {
      "command": "npx",
      "args": [
        "-y",
        "@hatcloud/linear-mcp"
      ],
      "env": {
        "LINEAR_API_KEY": "${user_config.linear_api_key}"
      }
    }
  }
}
